# Work Log — 2026-03-11

## 実施内容

### 改善提案対応 (8項目)

#### 1. プラン詳細ページ — ギャラリーを layouts API から取得
- `frontend/src/app/plans/[id]/page.tsx`
- `/api/media/images?category=plan-${id}` → `/api/layouts` + `plan.{id}.gallery` キーに変更
- Admin の Layout タブでドラッグ配置した画像がプランページに反映されるようになった

#### 2. 周辺エリアページ — レイアウト管理対応
- `frontend/src/app/api/layouts/route.ts` — `surroundings.spots` をデフォルトセクションに追加
- `frontend/data/page-layouts.json` — `surroundings.spots: []` 追加
- `frontend/src/app/surroundings/page.tsx` — layouts API からスポット画像取得、`spotImageUrls` で各スポット画像を上書き
- `frontend/src/app/admin/page.tsx` — Surroundings ページをレイアウト管理対象に追加

#### 3. アップロードプレビュー
- `frontend/src/app/admin/page.tsx`
- `uploadPreviews` state 追加
- ファイル選択時に `URL.createObjectURL()` でサムネイル生成、アップロード完了後クリア
- グリッド表示 + クリアボタン

#### 4. セクション枚数制限
- `frontend/src/app/admin/page.tsx`
- `SECTION_MAX` 定数追加 (Hero:10, Hotel:3, Surroundings:4, Gallery:50, Spots:9)
- `renderSection()` 内で枚数インジケーター表示、上限到達時 amber 色で警告

#### 5. 未使用画像の一括削除
- `frontend/src/app/admin/page.tsx`
- `handleBulkDeleteUnused` ハンドラー追加
- Images タブに「未使用を一括削除 (N件)」ボタン表示

#### 6. 反映前の差分確認ダイアログ
- `frontend/src/app/admin/page.tsx`
- `showPublishConfirm` state 追加
- 「反映する」ボタン → 差分確認モーダル（変更セクションと枚数変化一覧）→「確認して反映」でパブリッシュ

#### 7. 画像メタデータ編集
- `frontend/src/app/admin/page.tsx` — `editingMetaFile`/`metaForm`/`metaSaving` state、`handleSaveMeta` ハンドラー、ファイル名セルにペンシルアイコン、編集モーダル
- `frontend/src/app/api/media/[id]/route.ts` — PUT ハンドラーに `name` フィールドサポート追加

#### 8. 全ページレスポンシブ対応 (モバイル / タブレット / PC)

| ファイル | 主な変更 |
|---|---|
| `globals.css` | `.touch-target` ユーティリティ追加 |
| `page.tsx` (Home) | Hero h1/h2 text サイズ縮小、padding、Plans グリッド `sm:grid-cols-2` |
| `library/page.tsx` | Lightbox padding `px-8 sm:px-16`、閉じるボタン位置 |
| `plans/page.tsx` | Hero padding、グリッド `sm:grid-cols-2 lg:grid-cols-3` |
| `plans/[id]/page.tsx` | Hero h1 縮小、イテラリー縦積み→横並び、日番号モバイル非表示、Lightbox padding |
| `surroundings/page.tsx` | Hero padding、Stats p-4、交通グリッド `sm:grid-cols-3` |
| `admin/page.tsx` | Header 縦積み、タブ横スクロール、Plans テーブル `overflow-x-auto`、Layout タブサイドバー縦積み (`flex-col lg:flex-row`) |

## コミット

- `7eedeb2` — feat: 8 improvements — plan gallery, surroundings layout, admin enhancements, responsive design
- GitHub push 済み (master)
